const height = 800;
const width = 1000;
const cell_size = 40;
const no_rows = height/cell_size;
const no_cols = width/cell_size;


export default  {
    height,
    width,
    cell_size,
    no_rows,
    no_cols
}